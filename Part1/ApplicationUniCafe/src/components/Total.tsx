
/*
import { Part } from "../App";

export const Total = ({parts} : {parts: Part[]})  => {
  return (
    <div>
      <p>
        {parts.reduce((sum, part) => sum + part.exercises, 0)}
      </p>
    </div>
  );
};
*/
